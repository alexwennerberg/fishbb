Setup fishbb.org (cloud provider)

FEATURES:
implement registration

Search
Admin panel 

PAGINATION
  cleanup pagination (whitespace where > is) and add to top and bottom
  links on home and thread list should go to the post
  fix bugs

proper page titles
rate limiting -- add limit handler (go to error page) and limit certain actions by user
styling error page
inline form buttons not styled properly
input validation on all forms
distinguish notfound db error from other errors
login package should merge
cleanup err handling on db calls (over time)
implement change password

figure out an idiomatic time parsing strategy
why is link-button smaller? fix it generally

URL Hover to show full TS + Timestamp into a template snippet
Registration / invitations (closed and open signup)
- open signup: email verification
- closed signup: combination invitations and approval system
Post previews
Fine-tune CSS
Google Oauth
robots.txt
Rate limiting
Notifications (Including integration with PWA)
2fa with TOTP
JSON API
reporting system

TECH STUFF:
switch out slog-chi (it sucks) and configure better
remove fishbb.db from history (or check sensitivity)
panics -> log.Fatal
better db init / signal handling
Microformats

OPS/DEPLOY:
RRDtool integration / metrics
JS markdown editor
relicense to v3

Later goals:
language selection and localization
full Email interface / email notifications
